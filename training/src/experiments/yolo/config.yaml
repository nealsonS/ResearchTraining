valid_images: "../../data/combined_reannotated/valid/images"
valid_labels: "../../data/combined_reannotated/valid/labels"

data_config: "../../data/combined_reannotated/data.yaml"

conf_thresholds:
  - 0.6
  - 0.7
  - 0.8
  - 0.9

YOLO:
  model_id: "best.pt"
  epochs: 20
  imgsz: 640
  batch_size: 16
  inference_batch_size: 8
  save_model: false
  train: false
  eval: true


MLFLOW_EXPERIMENT_NAME: "YOLO"
MLFLOW_RUN: "Yolo Eval"

log_predictions: true
